# Pakiety do kompilacji EXE - będą wbudowane w EXE
# Duże pakiety ML/AI będą pobrane automatycznie przy pierwszym uruchomieniu

# Kompilacja
pyinstaller>=6.10.0

# Podstawowe biblioteki systemowe (wbudowane w EXE)
requests>=2.31.0
colorama>=0.4.6
pathlib2

# Web framework (wbudowane w EXE)
flask>=3.1.0
werkzeug>=3.0.1
flask-httpauth>=4.8.0
flask-limiter>=3.12

# API clients (wbudowane w EXE)
openai>=1.70.0
anthropic>=0.49.0
httpx>=0.27.2
aiohttp>=3.11.16

# Utilities (wbudowane w EXE)
python-dotenv>=1.0.1
coloredlogs>=15.0.1
beautifulsoup4>=4.13.3
markdown>=3.6  # For web UI documentation rendering
numpy>=1.24.0  # Core dependency used by assistant.py
watchdog>=3.0.0  # File system monitoring
ollama>=0.1.0  # Local LLM client
psutil>=5.9.0  # System monitoring (used by performance_monitor)

# Audio handling - sounddevice będzie pobrane automatycznie przy pierwszym użyciu
# USUNIĘTE: sounddevice>=0.4.6  (pobiera się z dependency_manager)

# Tylko do kompilacji
setuptools
wheel
